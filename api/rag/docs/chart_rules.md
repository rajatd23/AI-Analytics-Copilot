# Chart Rules

- If data contains a date/time column and a numeric metric → line chart (x=date, y=metric)
- If data contains a categorical column and numeric metric → bar chart (x=category, y=metric)
- If two numeric columns → scatter plot (x=col1, y=col2)
- If one numeric column only → histogram